<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geniusgames.layouttestapp">

    <application
        android:name=".Globals"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CodeLayout" />
        <activity android:name=".CreateWhileRunning" />
        <activity android:name=".ChangeLayoutWhileRunning" />
        <activity android:name=".ReflectTest" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".Settings" />
        <activity android:name=".SettingsFromExternalLayout" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".HorizontalScrollActivity" />
        <activity
            android:name=".ViewPagerActivity"
            android:label="@string/title_activity_view_pager"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
